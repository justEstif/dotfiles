# Archived: Zellij config

This config has been replaced by Ghostty's native splits/tabs/key tables.
Ghostty >= 1.3.0 provides built-in multiplexer features that eliminate the need for Zellij.

See: `ghostty/.config/ghostty/config`

To restore Zellij, reinstall it and symlink this config back:
  ln -sf $(pwd)/zellij/.config/zelli ~/.config/zellij
