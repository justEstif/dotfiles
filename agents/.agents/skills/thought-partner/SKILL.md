---
name: thought-partner
description: "Route conversational thinking requests to one focused framework. Seven modes: product validation (build-or-not), plan interrogation (grill-me), adversarial pushback (sycophancy), systems analysis (systems-thinking), request investigation (root-ask), stakeholder pre-alignment (stakeholder-prealignment), post-MVP strategic prioritization (post-mvp-crossroads). Use when user wants a sparring partner, says 'help me think', 'think with me', 'push back', 'challenge', 'stress test', 'what am I missing', 'grill me', 'build or not', 'devil's advocate', 'systems thinking', 'root cause', 'behind the request', 'get buy-in', 'stakeholder alignment', 'where do I go next', or any ambiguous thinking-mode request. If the user wants subagent fan-out, wide search, or multiple hypotheses investigated in parallel, use parallel-explore instead."
---

Pick the right mode. Load the reference. State why you chose it in one sentence before starting.

## Routing

1. **Product / startup / app idea to validate?**
   → **MANDATORY READ** `references/build-or-not.md`. Three gates: north star, core tech, defining constraint.

2. **Specific plan or design to resolve end-to-end?**
   → **MANDATORY READ** `references/grill-me.md`. Dependency-ordered interview until every branch is decided.

3. **Organizational complexity, feedback loops, multi-system dynamics?**
   → **MANDATORY READ** `references/systems-thinking.md`. Rumsfeld Matrix + Causal Loop Diagrams + debt cascades + fence investigation.

4. **Pushback on a claim, or want general adversarial thinking?**
   → **MANDATORY READ** `references/sycophancy.md`. Defaults to constructive disagreement. Includes YAGNI-at-scale pushback against hedging.

5. **Received a request that might not be the real need?**
   → **MANDATORY READ** `references/root-ask.md`. Investigate the underlying need through pain-point inquiry before committing to the stated solution.

6. **Need to get organizational buy-in before a meeting?**
   → **MANDATORY READ** `references/stakeholder-prealignment.md`. Nemawashi: systematic one-on-one pre-alignment so the meeting becomes ratification, not debate.

7. **Shipped MVP and staring at multiple competing paths?**
   → **MANDATORY READ** `references/post-mvp-crossroads.md`. Three-framework sequence: map opportunities (Opportunity Solution Tree), score options (RICE), balance horizons (3 Horizons of Growth).

8. **Handed a task and not sure how to approach it?**
   → Start with **root-ask** (is this actually the right task?) then **grill-me** (what decisions need resolving before you start?). Load one at a time.

9. **Ambiguous?**
   → Default `sycophancy`. Switch if the exchange reveals a clearer fit.

## Sequencing

Multiple modes apply? Run structuring → specific → granular:

**systems-thinking** → **post-mvp-crossroads** → **build-or-not** → **root-ask** → **grill-me** → **stakeholder-prealignment** → **sycophancy**

One mode per turn. Complete its line of questioning before switching.

## NEVER

- **NEVER answer a thinking request with generic advice before routing.**
  **Instead:** Pick the closest mode, load its reference, and state the routing reason in one sentence.
  **Why:** The skill's value is choosing the right thinking frame; skipping routing collapses seven distinct methods into vague coaching.

- **NEVER blend multiple frameworks in the same turn because several seem relevant.**
  **Instead:** Run one mode until its question line is complete, then switch only if the answer exposes a better fit.
  **Why:** Mixed frameworks create scattered questions and make it unclear what decision the user is actually making.

- **NEVER default to solution design when the real ask may be wrong.**
  **Instead:** Start with `root-ask` when the request may be a proxy for a deeper pain or organizational constraint.
  **Why:** Optimizing the stated solution can waste effort while the underlying need remains untouched.
